name: CI

on: [push, pull_request]

jobs:
  industrial_ci:
    strategy:
      matrix:
        env:
          - {ROS_DISTRO: "kinetic",
            ROS_REPO: "main",
            OPT_VI: "-vi",
            CATKIN_LINT: "true",
            ROSDEP_SKIP_KEYS: "denso_descriptions vs087_moveit_config denso_gazebo denso_execute denso_control phoxi_ros_pkg",
            CATKIN_LINT_ARGS: "--ignore unknown_package --skip-pkg eip_pkg"}

    runs-on: ubuntu-latest
    steps:
      - uses: webfactory/ssh-agent@v0.2.0
        with:
          ssh-private-key: ${{secrets.SSH_PRIVATE_KEY}}
      - uses: actions/checkout@v2
        with:
          repository: Nishida-Lab/cobotta_gripper_ros_pkg
          ref: master
          token: ${{secrets.ACCESS_TOKEN}}
      - uses: actions/checkout@v2
        with:
          repository: Nishida-Lab/iai_gripper_ros_pkg
          ref: master
          token: ${{secrets.ACCESS_TOKEN}}
      - uses: actions/checkout@v2
        with:
          repository: Nishida-Lab/mhand_ros_pkg
          ref: master
          token: ${{secrets.ACCESS_TOKEN}}
      - uses: actions/checkout@v2
        with:
          repository: Nishida-Lab/robotiq_3f_ros_pkg
          ref: master
          token: ${{secrets.ACCESS_TOKEN}}
      - uses: actions/checkout@v2
        with:
          repository: Nishida-Lab/tercero_ros_pkg
          ref: master
          token: ${{secrets.ACCESS_TOKEN}}
      - uses: 'ros-industrial/industrial_ci@master'
        env: ${{matrix.env}}
